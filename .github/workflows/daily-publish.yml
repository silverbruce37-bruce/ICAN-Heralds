name: Daily Publication

on:
  schedule:
    - cron: '0 0 * * *'
    - cron: '30 0 * * *'
    - cron: '0 1 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: daily-publish
  cancel-in-progress: false

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - name: Check if today's edition already published
        id: check
        run: |
          TODAY=$(TZ='Asia/Manila' date +%Y-%m-%d)
          if [ -f "data/daily-${TODAY}.json" ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "Today ($TODAY) already published — skipping."
          else
            echo "skip=false" >> $GITHUB_OUTPUT
            echo "Today ($TODAY) not yet published — proceeding."
          fi

      - name: Set up Python
        if: steps.check.outputs.skip != 'true'
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Create data directory
        if: steps.check.outputs.skip != 'true'
        run: mkdir -p data

      - name: Generate Daily Edition
        if: steps.check.outputs.skip != 'true'
        run: bash scripts/generate-daily.sh
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}

      - name: Check academy freshness
        if: steps.check.outputs.skip != 'true'
        id: academy
        run: |
          TODAY=$(TZ='Asia/Manila' date +%Y-%m-%d)
          FIRST=$(head -1 js/academy-data.js 2>/dev/null || echo "")
          if echo "$FIRST" | grep -q "${TODAY}"; then
            echo "status=fresh" >> $GITHUB_OUTPUT
            echo "✅ Academy data fresh for ${TODAY}"
          else
            STALE=$(echo "$FIRST" | grep -oE '[0-9]{4}-[0-9]{2}-[0-9]{2}' | head -1 || echo "unknown")
            echo "status=stale" >> $GITHUB_OUTPUT
            echo "stale_date=${STALE}" >> $GITHUB_OUTPUT
            echo "⚠️ Academy stale — ${STALE} (expected ${TODAY})"
          fi

      - name: Pre-warm image cache (Pollinations)
        if: steps.check.outputs.skip != 'true'
        run: bash scripts/prewarm-images.sh
        continue-on-error: true

      - name: Commit and Push
        if: steps.check.outputs.skip != 'true'
        run: |
          git config user.name "ICAN Heralds Bot"
          git config user.email "heralds@ican.academy"
          VOL=$(cat data/volume.txt 2>/dev/null || echo "?")
          COVER=$(python3 -c "
          import json, glob
          f = sorted(glob.glob('data/daily-*.json'))[-1]
          d = json.load(open(f))
          print(d.get('cover_story',{}).get('headline_en','Daily Edition')[:60])
          " 2>/dev/null || echo "Daily Edition")
          git add ican_news.html index.html sw.js data/ js/academy-data.js
          if git diff --staged --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git commit -m "Daily edition: $(TZ='Asia/Manila' date +%Y-%m-%d) VOL. $(printf '%02d' $VOL) — $COVER"
          # Push with rebase-retry (handles race when remote moved during this run)
          for i in 1 2 3 4 5; do
            if git push; then
              echo "Push succeeded on attempt $i."
              exit 0
            fi
            echo "Push rejected (attempt $i) — rebasing and retrying..."
            git pull --rebase origin main || { echo "Rebase failed"; exit 1; }
            sleep $((i * 2))
          done
          echo "Push failed after 5 attempts."
          exit 1

      - name: Notify Telegram on success
        if: steps.check.outputs.skip != 'true' && success()
        run: |
          TODAY=$(TZ='Asia/Manila' date +%Y-%m-%d)
          VOL=$(cat data/volume.txt 2>/dev/null || echo "?")
          MSG="📰 iCAN Herald 발행 완료 — ${TODAY} VOL. $(printf '%02d' $VOL)%0Ahttps://ican-heralds.vercel.app/"
          curl -sS -X POST "https://api.telegram.org/bot${TG_TOKEN}/sendMessage" \
            -d "chat_id=${TG_CHAT_ID}" -d "text=${MSG}" > /dev/null || true
        env:
          TG_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

      - name: Notify Telegram on academy stale
        if: steps.check.outputs.skip != 'true' && steps.academy.outputs.status == 'stale' && success()
        run: |
          TODAY=$(TZ='Asia/Manila' date +%Y-%m-%d)
          STALE="${{ steps.academy.outputs.stale_date }}"
          RUN_URL="https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          MSG="📚 Academy 갱신 실패 — ${TODAY}%0A"
          MSG="${MSG}학습 콘텐츠(academy-data.js)가 ${STALE}자 고정 상태%0A"
          MSG="${MSG}(대개 Gemini 무료 티어 쿼터 초과)%0A%0A"
          MSG="${MSG}🛠 수동 복구 — Mac 터미널에서:%0A"
          MSG="${MSG}cd ~/Documents/Documents%20-%20W1%20-%20Mac%20Studio(64gb)/ICAN-Heralds %26%26 bash scripts/regen-academy.sh%0A%0A"
          MSG="${MSG}ℹ️ Daily 기사는 정상 발행됨. LEARN 버튼만 5일 전 콘텐츠 표시 중.%0A${RUN_URL}"
          curl -sS -X POST "https://api.telegram.org/bot${TG_TOKEN}/sendMessage" \
            -d "chat_id=${TG_CHAT_ID}" -d "text=${MSG}" > /dev/null || true
        env:
          TG_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

      - name: Notify Telegram on failure
        if: failure()
        run: |
          TODAY=$(TZ='Asia/Manila' date +%Y-%m-%d)
          RUN_URL="https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          MSG="⚠️ iCAN Herald 발행 실패 — ${TODAY}%0A${RUN_URL}"
          curl -sS -X POST "https://api.telegram.org/bot${TG_TOKEN}/sendMessage" \
            -d "chat_id=${TG_CHAT_ID}" -d "text=${MSG}" > /dev/null || true
        env:
          TG_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
